# go-build-containers

## Setup

`dep ensure`

## Build

`make build`

> Binary will be in `output/`

## Cluster

`make run-cluster`

> Will build the binary, then mount it in four containers and run it.
